# TODO — Universe Anime Planet Bot

Этапы 0–13 (инфраструктура, гача, магазин, кланы, пасс, донат, админка, Mini App) завершены —
история в git. Дальше — новый блок функций (2026-08-06, полный план см. в истории чата/плане
сессии). Порядок реализации: A → H → B → D → E → C → F → G.

## Этап A — Реферальная система (доделка) — готово 2026-08-06
- [x] `User.referred_by_id` (FK users.id, SET NULL) — выставляется один раз при `/start
  r_<user_id>` (новый префикс, отдельно от админских `ref_<код>`)
- [x] Награда рефереру — 50 коинов + 50 тикетов, начисляется после ПЕРВОЙ крутки приглашённого (не сразу при /start — анти-абьюз)
- [x] 10% с донатов реферала — начисляется рефереру при каждом успешном платеже, пока связь есть
- [x] Экран "🔗 Рефералы" в профиле — заменить заглушку: ссылка, число приглашённых/играющих/донатеров/подписчиков/владельцев пасса, заработано с рефералов
- [ ] Миграция: `users.referred_by_id` — сгенерировать и накатить на сервере (`alembic revision --autogenerate` + `upgrade head`)

## Этап H — Обменник клана: тикеты и коины — готово 2026-08-06
- [x] `exchange_currency()` в `services/clan/exchange.py` — обобщить с пыли на пыль/тикеты/коины
- [x] Экран выбора валюты перед вводом `@username сумма`
- [x] Без миграций

## Этап B — Ежедневный бонус (streak 1-7 дней) — готово 2026-08-06
- [x] `User`: `daily_bonus_streak`, `daily_bonus_claimed_at`, `notify_daily_bonus`, `daily_bonus_notified_at`
- [x] Логика: заберёт вчера → +1 к streak (макс 7); пропустил день (>48ч) → сброс на 1
- [x] Награда по дню — таблица в `config/game.py` (день 7 — 5 тикетов + 150 пыли, растущая шкала)
- [x] UI: 7 ячеек (✅/🎁/▫️), красиво оформлено
- [x] Тумблер уведомления в Настройках, пуш через `services/notify.run_sweep` (раз в окно готовности, не по фиксированному интервалу)
- [x] Кнопка "🎁 Ежедневный бонус" в профиле — заменить заглушку
- [ ] Миграция: 4 колонки на `users` — сгенерировать и накатить на сервере

## Этап D — Админ-панель: управление админами — готово 2026-08-06
- [x] Кнопка "👑 Админы" в `admin_menu()`, видна только `is_config_admin` (ADMIN_IDS)
- [x] Список доп.админов + поиск игрока (переиспользовать `resolve_player`) → выдать/забрать `is_admin`
- [x] Уведомление игроку при выдаче/отзыве прав
- [x] Без миграций (`User.is_admin`, `set_is_admin()` уже есть)

## Этап E — Админ-панель: полное удаление БД
- [ ] Только `is_config_admin`, TRUNCATE всех таблиц приложения (схема/alembic_version не трогаем)
- [ ] Подтверждение — два последовательных нажатия (с TTL-защитой от случайного повтора спустя время)
- [ ] Уведомление всем ADMIN_IDS после очистки
- [ ] Без миграций

## Этап C — Ежедневные задания (пул 30, 5 активных)
- [ ] 30 определений заданий (текст/метрика/target/награда) — 3 уровня сложности
- [ ] Таблица `daily_quests` (user_id, slot 0-4, quest_code, progress, target, claimed, assigned_at)
- [ ] Прогресс — живой пересчёт при открытии экрана (не инкремент на каждое действие)
- [ ] Реролл: 1 "переролить все" в сутки + 2 реролла отдельных заданий в сутки
- [ ] Прогресс-бар — квадратики ⬛⬜, не текстовый `▓░`
- [ ] Кнопки "забрать конкретное" и "забрать все выполненные"
- [ ] Тумблер уведомления + пуш "задания обновлены" через `run_sweep`
- [ ] Точка входа в меню — решить при вёрстке
- [ ] Миграция: таблица `daily_quests` + 4 колонки на `users`

## Этап F — Ивенты
- [ ] `Universe.is_event`, таблица `events` (code, title, universe_code, is_active) — 2 записи: lookism_battle_planet, halloween
- [ ] Только один активный ивент одновременно (partial unique index)
- [ ] Механика крутки: 0.05% шанс ивент-карты поверх обычной крутки любой вселенной, пока ивент активен
- [ ] Ивент-карты — 7000 UBP, грузятся через `seed_cards.py` (папка `assets/cards/event_<code>/7000UBP/...`)
- [ ] Категория "Ивенты" в коллекции бота и в Mini App
- [ ] Админ-кнопка "🎉 Ивенты" — список, вкл/выкл (доступно обычным `is_admin`)
- [ ] Миграция: `universes.is_event`, таблица `events`

## Этап G — `/help`
- [ ] Большой интерактивный гайд, разделы по всем механикам бота, кнопки вперёд/назад
